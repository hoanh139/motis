schema {
  query: QueryType
}

scalar DateTime

input Location {
  lat: Float!
  lon: Float!
}

type Trip {
  departure: DateTime
  arrival: DateTime
}

type QueryType {
  trip(
    from: Location!
    to: Location!
  ) : Trip!
}