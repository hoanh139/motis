schema {
  query: QueryType
}

scalar DateTime

input Location {
  latitude: Float!
  longitude: Float!
}

type Trip {
  departure: DateTime
  arrival: DateTime
}

type QueryType {
  trip(
    from: Location!
    to: Location!
  ) : Trip!
}